# github-user-activity-cli
CLI and Django-based web application to fetch and visualize GitHub user activity using GitHub API. Displays commits, events, and repositories in a clean and structured format.
